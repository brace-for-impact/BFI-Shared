import * as dockerServices from './docker.services';
import * as kafkaServices from './kafka.services';
import * as hostServices from './host.services';
import * as nodeServices from './node.services';

export default {
    dockerServices,
    kafkaServices,
    hostServices,
    nodeServices
}